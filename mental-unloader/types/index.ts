export type ShoppingItem = {
  id: string;
  text: string;
};

export type Task = {
  id: string;
  text: string;
};

export type CalendarEvent = {
  id: string;
  title: string;
};

export type PackingItem = {
  id: string;
  text: string;
};
